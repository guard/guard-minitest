source 'https://rubygems.org'

gem 'rake'
gem 'bundler'
gem 'mocha'
gem 'guard'

# The test group will be
# installed on Travis CI
#
group :test do
  gem 'minitest', '~> 3.0'
  gem 'coveralls', :require => false
end
